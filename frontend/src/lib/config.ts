// Configuration settings for the application

// Set this to false when you want to connect to the real backend
// Set to true for frontend-only development with mock data
export const USE_MOCK_API = false;

// Other configuration settings can be added here
export const CONFIG = {
    // Default URL for demo purposes
    DEMO_URL: "https://www.example.com/article",

    // Animation timing (ms)
    STATUS_CHANGE_INTERVAL: 1500,

    // API timeouts (ms)
    API_TIMEOUT: 30000,
}; 